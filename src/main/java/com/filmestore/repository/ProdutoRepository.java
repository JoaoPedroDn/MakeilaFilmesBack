package com.filmestore.repository;

import org.springframework.stereotype.Repository;

/**
 * Este repositório foi DESATIVADO.
 * A leitura do CSV agora é feita diretamente no ProdutoService.
 * Mantido aqui apenas para não dar erro de arquivo faltando, se houver referências antigas.
 */
@Repository
public class ProdutoRepository {
    // Vazio de propósito.
}